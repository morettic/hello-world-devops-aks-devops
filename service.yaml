apiVersion: v1
kind: Service
metadata:
  name: hello-world-app
  labels:
    app: hello-world-app
spec:
  type: ClusterIP
  selector:
    app: hello-world-app
  ports:
    - name: http
      port: 81
      targetPort: 8080